%h1 History

.card.text-bg-info{ style: "width: 100%;" }
  .card-body
    %h5.card-title Previous Wagers
    %p.card-text Starting Balance: $0.00
    %p.card-text Current Balance: $100.00
  %ul.list-group.list-group-flush
    - @wagers.each_with_index do |wager, index|
      %li.d-flex.list-group-item.align-items-center{ id: "wager-#{wager.id}", class: ("bg-info-subtle" if index % 2 == 0) }
        = wager.placed_at.strftime("%-m/%-d %l:%M%P")
        .ms-5.flex-fill= wager
        - if wager.win?
          .text-success= "Won: #{ActionController::Base.helpers.number_to_currency(wager.net)}"
        - elsif wager.loss?
          .text-danger= "Loss: #{ActionController::Base.helpers.number_to_currency(wager.net)}"
        - else
          .text-secondary= "Push"
