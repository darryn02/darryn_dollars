<%= yield :javascript %>

<%= render "analytics" %>

<% if Rails.env.test? %>
  <%= javascript_tag do %>
    window.addEventListener("load", function () {
      if (window.jQuery) {
        window.jQuery.fx.off = true;
        window.jQuery.ajaxSetup({ async: false });
      }
    });
  <% end %>
<% end %>
