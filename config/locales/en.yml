en:
  activerecord:
    errors:
      models:
        wager:
          attributes:
            amount:
              greater_than_or_equal_to: "must be at least $%{count}"
  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %l:%M%P"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Darryn Dollars
    games:
      index: Current Lines
